import React, { FunctionComponent } from 'react';
import { Screen, Text } from 'src/components';

const SignUp: FunctionComponent = (): React.JSX.Element => {
  return (
    <Screen>
      <Text text="Sign Up" />
    </Screen>
  );
};

export default SignUp;
