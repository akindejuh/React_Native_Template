import React, { FunctionComponent } from 'react';
import { Screen, Text } from 'src/components';

const Home: FunctionComponent = (): React.JSX.Element => {
  return (
    <Screen>
      <Text text="Home" />
    </Screen>
  );
};

export default Home;
